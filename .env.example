# API Configuration
VITE_API_URL=http://localhost:8080/api

# Supabase (optional - only needed once real auth is wired up; unused for now,
# see src/services/supabaseClient.js). Use the anon/public key, never service_role.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# App Configuration
VITE_APP_NAME=TeamEnergy
VITE_APP_VERSION=1.0.0
